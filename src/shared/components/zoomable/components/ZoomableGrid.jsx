import styled from 'styled-components'

const ZoomableGrid = styled.div`
  display: grid;
  grid-gap: var(--grid-gutter) var(--grid-gutter);
  grid-template-columns: 1fr 1fr 1fr 1fr 1fr 1fr;
`

export default ZoomableGrid
