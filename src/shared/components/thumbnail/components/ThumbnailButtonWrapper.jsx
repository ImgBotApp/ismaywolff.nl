import styled from 'styled-components'

const ThumbnailButtonWrapper = styled.div`
  bottom: 0;
  color: var(--color-white);
  padding: var(--size-small);
  position: absolute;
  right: 0;
`

export default ThumbnailButtonWrapper
